java install 

    Method 1: sudo apt update
            	sudo apt install openjdk-11-jdk -y
    Method 2: Check Java path
              which java
    If installed, you’ll get something like:
              /usr/bin/jav
